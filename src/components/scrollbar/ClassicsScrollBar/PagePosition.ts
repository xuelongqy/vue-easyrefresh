/**
 * 页面位置
 */
class PagePosition {
    public pageX: number
    public pageY: number

    constructor(pageX: number, pageY: number) {
        this.pageX = pageX
        this.pageY = pageY
    }
}

export default PagePosition
